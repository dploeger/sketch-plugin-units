{
  "name": "units",
  "version": "1.0.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "repository": "https://github.com/dploeger/sketch-plugin-units",
  "skpm": {
    "name": "units",
    "manifest": "src/manifest.json",
    "main": "de.dieploegers.resize.sketchplugin"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "prepare": "skpm-build",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.2.0"
  }
}
